__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
out/

# The analysis corpus IS redistributed: openFDA is CC0, it is 635 filing events
# (2 MB), and the paper's null result cannot be checked without it.
#   data/deviations_maude.jsonl   the analysis corpus
#   data/maude_provenance.json    endpoint, query, years, count, timestamp
#
# Not redistributed: the 172 MB raw download and the synthetic teaching corpus,
# both regenerated by `run.py fetch` / `run.py setup --source synthetic`.
data/maude_raw.jsonl
data/maude_fixture.jsonl
data/deviations.jsonl
